name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-universal:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Build arm64
        run: swift build -c release --arch arm64

      - name: Build x86_64
        run: swift build -c release --arch x86_64

      - name: Create universal binary
        run: |
          mkdir -p release
          lipo -create \
            .build/arm64-apple-macosx/release/sad \
            .build/x86_64-apple-macosx/release/sad \
            -output release/sad
          chmod +x release/sad

      - name: Create tarball
        run: |
          cd release
          tar -czf sad-macos-universal.tar.gz sad
          shasum -a 256 sad-macos-universal.tar.gz > sad-macos-universal.tar.gz.sha256

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/sad-macos-universal.tar.gz
            release/sad-macos-universal.tar.gz.sha256
          generate_release_notes: true
